steps:
  # Build the migration image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/kids-activity-tracker-2024/migration:latest', '-f', 'deploy/Dockerfile.migration', '.']
  
  # Push the migration image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/kids-activity-tracker-2024/migration:latest']

images:
  - 'gcr.io/kids-activity-tracker-2024/migration:latest'

timeout: '600s'